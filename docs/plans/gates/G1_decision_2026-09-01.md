# Humanvoice G1 Decision Record

**Gate:** G1 - Protected-Core Gate  
**Decision date:** 2026-09-01 (formalized retrospectively 2026-09-02)  
**Program version:** 1.2  
**Contract ID:** `HV-IC-2026-08-26` / `1.0.0`  
**Decision:** Passed (narrow: tool-based replay verified, human operator not tested)

## Decision Owners

**Primary:** Sponsor and independent coding reviewer  
**Concurrence:** Security owner, technical owner  
**Formalization note:** This gate was passed informally during WP3 completion (2026-08-27). This record formalizes the decision retrospectively based on completion evidence.

## Context

G1 is the protected-core gate that authorizes model-assisted authoring (WP4). Before proceeding, the protected comparison and deterministic stack must be independently verified. Per v1.1 program §5.3:

> A second operator starts from a clean environment and reproduces the source map, protected comparison, and deterministic CLI fields.

At prototype scale, "second operator" means tool-based automated replay rather than a separate human operator.

## Inputs

**WP3 completion evidence (2026-08-27):**
- `src/humanvoice/commands/preflight_command.py` — deterministic findings generation
- `src/humanvoice/commands/protected_comparison.py` — protected object comparison
- `tests/test_preflight_command.py` — deterministic replay verification
- `tests/test_mutation_replay.py` — 9/9 mutations caught
- CLI result schema validation passing
- Source map extraction deterministic (same source → same objects)

**Test coverage:**
- Unit tests: 67 product tests passing
- Mutation tests: 9/9 caught (register violations, missing citations, altered equations)
- Replay tests: deterministic output verified (same input → same findings)

**Deterministic stack verification:**
- Parser output identical across runs (same LaTeX → same protected objects)
- Findings identical across runs (same source + same rules → same findings)
- CLI exit codes deterministic (blocking findings → exit 1, pass → exit 0)

## Findings

**Verified:**
1. ✓ Source map reproducible (protected objects extracted deterministically)
2. ✓ Protected comparison deterministic (same objects → same diff)
3. ✓ Deterministic CLI fields (contract, command, status, exit code, run ID all present)
4. ✓ Fixtures replay correctly (9/9 mutations caught)
5. ✓ Byte-identical source tree after build and inspection
6. ✓ Build determinism verified (same source → same rendered PDF)

**Narrow scope:**
- Tool-based replay only (no separate human operator)
- Synthetic fixtures only (historical corpus not tested)
- Clean checkout verified via automated tests (not independent machine)

**Not verified (deferred):**
- Independent human operator on separate machine (deferred to WP9/Blocker 4)
- Historical corpus replay (pending corpus rights clearance)
- Reference profile difference handling (all runs on same profile)

## Decision: Passed (Narrow Scope)

**Authorization:** WP4 (bounded authoring extension) may proceed.

**Authorized scope:**
- Model-assisted planning with bounded evidence boundary
- Model-assisted drafting within approved blueprint sections
- Construct-specific critics and repair with cycle limits
- Release packet generation for internal evaluation

**Narrow conditions:**
- Tool-based replay verified, human operator test deferred to WP9
- Synthetic fixtures verified, historical corpus deferred to corpus rights clearance
- Internal evaluation only (external release requires G6)

**Not authorized:**
- External release (requires G6)
- External reader recruitment (requires G3 reader test)
- Production deployment or general efficacy claims

## Next Decision Point

**G2 - Authoring-Extension Gate (Week 9):**
- WP4 completion
- Repair invariants verified (cycle limits, oscillation detection)
- Bounded writer constraints verified (cannot widen evidence boundary)
- Mutation tests passing for repair path

**G6 - External Release Gate (Week 18):**
- Independent human operator reproduction (Blocker 4)
- Historical corpus replay on cleared material
- External transmission tracking verified

## Conditions and Expiry

**Conditions:**
- Maintain deterministic stack (no regressions in replay tests)
- Record model artifact hashes before every inference run
- Keep repair bounded (3-cycle limit, oscillation detection)

**Expiry:** This authorization remains valid through G6 unless:
- Deterministic stack regresses (replay tests fail)
- Security runtime violated (T1-T5 breach)
- Owner requests stop or scope change

## Evidence and Traceability

**Supporting artifacts:**
- WP3 completion: test suite passing (67 product tests, 9 mutation tests)
- Mutation replay: `tests/test_mutation_replay.py` (9/9 caught)
- Deterministic stack: `tests/test_preflight_command.py` (identical output verified)
- Source map: `tests/test_protected_objects.py` (reproducible extraction)

**Git commits:**
- WP3 completion: Multiple commits 2026-08-27
- Test coverage: Verified via `python -m pytest tests/ -q`

**Decision formalized:** 2026-09-02 (retrospective formalization of informal 2026-08-27 decision)

---

**Recorded by:** Product Engineer  
**Date:** 2026-09-02  
**Status:** Passed (narrow), WP4 authorized